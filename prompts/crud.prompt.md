# Prompt: Endpoint with pagination and filters

Create a GET endpoint in Express that lists users from a simulated database, with support for pagination (limit and offset) and filters by role (admin, user).
